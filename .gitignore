# Python
__pycache__/
*.pyc

# Leftover local audio cache from the removed desktop front-end - nothing
# regenerates this anymore, just ignore it if it's still on disk.
assets/

# Personal local data - never commit saves or scores
save.json
settings.json
highscores.json
speedrun_scores.json
daily.json
run_history.json
bestiary.json
achievements.json
lore_journal.json

# A player-imported texture pack, extracted from a .edtp via
# ui/texturepack.py's extract_pack_zip_bytes() (currently unreachable from
# any live front-end - the web build's Import is session-only in memory
# instead) - personal, never shipped in the repo.
/imported_textures/

# itch.io packaging output (scripts/build_itch_zip.sh) - zip + staging dir
/dist/

# Private lore canon and ARG masters - never publish (the repo is public,
# and a mystery box dies the day its answers are readable on GitHub)
/lore/
