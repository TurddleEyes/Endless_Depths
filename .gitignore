# Python
__pycache__/
*.pyc

# Generated audio cache (rebuilt automatically on first launch)
assets/

# Personal local data - never commit saves or scores
save.json
settings.json
highscores.json
speedrun_scores.json
daily.json
run_history.json
bestiary.json
achievements.json
lore_journal.json

# A player-imported texture pack (ui/app.py "Import Texture Pack" /
# web's session-only equivalent) - personal, never shipped in the repo.
/imported_textures/

# itch.io packaging output (scripts/build_itch_zip.sh) - zip + staging dir
/dist/

# Private lore canon and ARG masters - never publish (the repo is public,
# and a mystery box dies the day its answers are readable on GitHub)
/lore/
